# Work packages

| WP | Scope | Done (Have) |
|---|---|---|
| 0 | `AppLauncher` Felix 7.0.5, Gogo 17179, `weasis:info -v` → 4.7.3 | smoke `lb` Felix + core; `weasis:ui` `-q` quit / `-v` window on top Have (`WeasisUiCommand`; null-safe quit when not activated) |
| 1 | Core SDK, dummy `SeriesViewerFactory` | blank `ViewerPlugin`; Help > Keyboard Shortcuts live map (`ShortcutPrefView` / `ShortcutManager`, SHORTCUTS.md defaults, no overlay key); Help > System resources diagnostic (heap + native image memory, `ResourceMonitorDialog`) |
| 2 | OpenCV fragment, imageio, dicom-codec | EVR LE MONOCHROME2 CT W/L paints |
| 3 | Import + Explorer | synthetic DICOMDIR; File>Import DICOM and DICOM CD; File>Export DICOM files/ZIP/DICOMDIR; thumbnail Ctrl+click / Shift+click / Ctrl+A / Enter; last click raises download priority; PatientPane/StudyPane/PatientSelectionManager group patient → study → series; `LoadDicom`/`LoadDicomDir` open Part-10 / DICOMDIR into `DicomModel`; `LoadDicomObjects` Part-10 file list (no folder walk); `PluginOpeningStrategy` SOP→MIME→factory with one `DicomViewerPlugin` tab per patient and SEG/RT/PR/KO overlay (no tab); `main/DicomExplorer` load+open; `DicomSeriesHandler` series buckets; `HangingProtocols` MG 2×2 vs CT 1×1; `MimeSystemAppFactory` `application/pdf` and `video/*` (not `image/dicom`); explorer `JList` series-row and `SeriesThumbnail` left-press arm `dragging()` (no native `exportAsDrag`); AWT `MOUSE_RELEASED` `hangAtScreen` fills empty 2×2 `View2d` (no new tab); `beginDrag`/`hangAtScreen`/`lastMiss` in `/tmp/weasis-dnd.log`; explorer `weasis.dicom.explorer.filter.mode` TEXT/DATE/MODALITY (`main/SeriesFilter`; clone-only `explorer.SeriesFilter` same modes) |
| 4 | View2d LUT chain, `dcmview2d:*` | W/L paints; `FlipOp` horizontal flip + `WindowOp` linear/SIGMOID VOI on `BufferedImage`; `RotationOp` 0/90/180/270 raster + `ZoomOp` 0/−200/−100/factor + `CropOp` region copy (`ImageOpHaveTest`); zoom −200/−100 toolbar + screenshot PNG/JPEG Have (headless-safe `ScreenshotDialog`); rotation 0/90/180/270 chrome; ViewerToolBar mouseLeftAction tokens; T/W/S/Z/R left-mouse keys; C cine toggle; Q context menu; P `ImagePrint`; cine Play/Stop frame advance + loop; Space/I annotation visibility FULL/MINIMAL/HIDDEN; CalibrationView session mm/pixel ≠ monitor pitch (MX-07); image/series/study/patient keys; Alt+arrows pan; F11 fullscreen; Alt+S SEG; 0–9 VOI presets; `VOILUTSequence` table items beyond linear WC/WW + `VOILUTFunction` SIGMOID (`LutPipeline` / `WindowLevelPainter`); Ctrl/Ctrl+Shift drag accelerate; Ctrl/Alt do not steal letter actions (except Alt+P); Alt+R/L 90° rotate; Alt+F flip; Ctrl+Space next left action; Ctrl+/- zoom; Ctrl+Enter reset zoom; Tab/Shift+Tab layout views; LutToolBar Gray/Inverse; ResetTools `dcmview2d:reset` tokens; `ViewCanvas`/`Canvas` core types (`DefaultView2d` implements); `ViewerPrefView`/`ViewerPrefFactory` persist documented W/L and PR overlay tokens (`weasis.level.inverse`, `weasis.color.wl.apply`, `weasis.apply.latest.pr`, `weasis.toolbar.mouse.left` plus middle/right/wheel); `DcmHeaderToolBar` dumps selected image dataset tags; `DicomFieldsView` limited clinical subset vs all dump (Pixel Data as `[OW]`) plus `AbstractTagSearchPanel` / `TagSearchTablePanel` / `TagSearchDocumentPanel` keyword/hex/value search; **no MPR** |
| 5 | Draw & Measure, KO, PR/GSPS | every graphic `buildShape` non-null when complete; MeasureToolBar D/A/Y/G/B buttons + click-drag-release distance/angle Have; click-select / Shift+click / rubber-band / Ctrl+A/D / Delete Have; KO/PR files; Key Object star/filter chrome + K toggle; `XmlGraphicModel` JAXB round-trip (`@XmlRootElement` types, JAXB-OSGi @7); `PrGraphicUtil` maps GSPS Graphic Object POINT/POLYLINE/INTERPOLATED/CIRCLE/ELLIPSE plus compound RECTANGLE and text objects; `InterpolatedPath2D` Catmull-Rom through INTERPOLATED points; DISPLAY units scale by columns/rows |
| 6 | Docking Frames, FoR+manual sync, lens, 2D 3D-cursor | FoR vs MANUAL MX-14; docking-frames **Active** @10 (FlatLaf extras on system classpath); MigLayout Core+Swing **Active** @7; JAXB-OSGi **Active** @7; 2D lens `ZoomWin` factor Have; Display/Image dockables + InfoLayer 3-state; histogram dock bind + RGB/HSV/HLS + ROI + W/L output; `LayerType` IMAGE/MEASURE/DRAW/…; 3D cursor (H/crosshair) + PixelInfo + FoR/manual synch; Display `LayerItem` / `LayerAnnotation`; Mini Tool sliders + panner; ImageRegionStatistics on selected closed graphic; `GraphicsPane` paints bound drawings; central-panel Ctrl+Tab/Shift+Tab cycle, Ctrl+M maximize/restore, Ctrl+W close, Ctrl+E externalize, Ctrl+N normalize, Ctrl+Shift+E docking list; remaining viewer-tab drag onto `viewer-work` edge uses real `CLocation.working()` east/west/south/north (first tab stays on `viewer-tabs`; hang 1×2 is not this split; series `dragging()` does not split) |
| 7 | MPR / CPR / MIP | `mpr` types; MIP None/Min/Mean/Max; Alt+X/C/V center / show-center / show-crosshair on selected plane; Ctrl+Alt variants for all planes; Ctrl+Alt+B cycles MIP; Alt+wheel thickness on selected axis; Basic3DToolBar opens `MprContainer`; `CurveSampler` uniform arc-length; straightened CPR (`CurvedMprBuilder`/`CurvedMprView`); `ObliqueMpr` origin+u+v; curve-perpendicular `CrossSectionImageIO`; Shoemake `ArcBallController` rotates `AxesControl` `u`/`v`; `VolImageIO` reconstructs planes; `CopyPixelsTask` → signed-16 `RawImageIO`; `DerivedStack.rawSlice`; `BuildContext` Volume+axis+index+MIP; `SliceCanvas` paints `VolImageIO` `BufferedImage`; `SegVolumeBuilder` rasterizes SEG frames into `VolumeByte`/`VolumeShort`; `MprGeometryModule` origin/row/column + plane index; `SplatContext` orthogonal/oblique MIP splat; `CrossLineGraphic` plane lines + MIP slab; MIP `SeriesBuilder` axial frames from `Volume` + `MipView.Type` |
| 8 | Fusion | `FusionController.targetViews`; SUVbw; IPP-Z `FusionVolumeBuilder`; nearest-Z `FusionSliceMatcher`; resample onto CT FoR (`FusionVolumeResampler` + identity/voxel-shift `FusionRegistration`); same-FoR overlap `FusionCompatibility`; min/max `FusionWindowEstimator`; Hot Iron/PET `FusionColorScale` + `FusionColorBar`; `FusionMeasurableLayer` overlay mm adapter when the fusion stack has content |
| 9 | JOGL 3D VR | MX-15 Have: GL 3.3+; refuse llvmpipe; N/A no GPU; jogamp Active @120, native Resolved @121; 3D toolbar/LUT/VolumeTool apply COMPOSITE/MIP/MINIP/ISO + CT presets to selected `View3d` |
| 10 | SR AU ECG RT SEG | MIME + factories Active on `lb`; SR Content Sequence TEXT/NUM/CODE/CONTAINER/IMAGE display in `SRView`/`SRContainer`; `SRImageReference` resolves IMAGE content items to SOP Instance UID; `SrToolBar` SR viewer chrome; `EditorPanePrinter` prints HTML SR view; ECG Waveform Sequence 8/16-bit samples to millivolt 2/4/12-lead `WaveView`; AU Waveform Sequence 8/16-bit interleaved PCM in `AuView` with play/pause/stop; RT Structure Set CLOSED_PLANAR contours by Z in `RtDisplayTool`; SEG overlay dockable `SegmentationTool`; `SegComponentFactory` mask `SegGraphic` overlays on `ViewCanvas`; `SegPrefView` opacity/fill/contour (Alt+S hide/show already WP-4); `SegRegionLocator` mask/overlay hit; `UISegmentationVolumeBuildExecutor` invokes `SegVolumeBuilder` off-EDT (`Future`/`Volume`) |
| 11 | QR C-FIND/MOVE/GET, WADO, QIDO/STOW, send, ISO, print | network Have tests; XML/JSON WADO manifest + WADO-RS retrieve + MX-10/11 caps; DICOM Print N-CREATE Film Session/Box/Image Box + N-ACTION Print (type 1); film-session dialog Have; Q/R import page C-FIND keys + result tree + C-MOVE/C-GET plan + `GracefulCancel`; send page C-STORE vs STOW-RS multipart; `AbstractDicomNode` host/AET/port defaults `DCM4CHEE:11112`; `DicomNodeListView` lists nodes; `DicomNodePrefFactory` prefs page; `DicomNodeDialog` DIMSE apply/add/replace; `DicomWebNodeDialog` http(s) QIDO/WADO/STOW + headers; `AbstractListEditor` add/edit/delete; `HttpHeadersEditor` duplicate-name replace; `AuthenticationPersistence` / `AuthMethodDialog` / `AuthenticationEditor` `weasis.auth.ids` round-trip; `DicomWebNode.requestHeaders()` auth wins |
| 12 | Dicomizer profile | `dicomizer.json` overlay; acquire convert Have; Gogo **17181** smoke (`scripts/dicomizer-gogo-smoke.sh`) when overlay is applied; desktop Gogo stays **17179**; `acquire:patient` `-x\|-i\|-s\|-u` `<tags>` DICOM-keyword XML including nested `IssuerOfAccessionNumberSequence` onto album `Global`; photo editor rotate → crop → contrast (`AcquireAction` + `AcquireImageValues`); known mm length on `CalibrationGraphic` → mm/pixel; explorer `AcquireMetadataTableModel` + global/series/image panels from `weasis.acquire.meta.{global,series,image}.{display,edit,required}`; PersonName view/cell editor wired to `PersonNameEditor`; AnatomicRegion code/label on image and series meta; editor annotate reuses WP-5 `MeasureTool` graphics; `MetadataAction` required gate; exclusive `AcquireActionButton` chrome plus Apply/Cancel; import/central thumbnail selection; `AcquireImportDialog` grouping; publish tree/dialog/panel (`PublishDicomTask` C-STORE vs local export); browse `FileSystemDrive`/`MediaSource` still listing; `BrowsePanel` source select; `ImportPanel`/`ImportTask`; `SeriesButton`/`AcquireTabPanel` series switch; `EditionToolFactory` → Edition; `SeriesGroup` named `AcquireImageInfo`/`AcquireMediaInfo`; `AcquireNewSeriesDialog` NAME grouping; `MediaImporterFactory` still import into `AcquireManager`; `Global`/`DefaultTaggable` patient tags; `AcquireImageStatus` TO_PUBLISH vs PUBLISHED; `AutoLevelListener` histogram stretch onto `AcquireImageValues` |
| 13 | `weasis://`, `$dicom:get`/`-rs`, non-DICOM profile | URI + `$weasis:config` parser; `dicom:get`/`rs`/`close`; `dicom:close` `-a` / `-p ID` / `-y` study UID / `-s` series UID removes matching instances from fixture `DicomModel`; `image:get`/`close` Have (local `-f` ImageIO, remote `-u` no fetch); ImportToolBar stills via ImageIO; `non-dicom-explorer.json`; base-explorer `AbstractThumbnailList` / `JIThumbnailListPane` Ctrl+click / Shift+click / Ctrl+A / Enter; `JIThumbnailCache` URI LRU; `DefaultThumbnailList` / `AThumbnailListPane`; `ThumbnailList` / `IThumbnailListPane` / `AThumbnailModel`; `ThumbnailRenderer` paints `ThumbnailIcon` from that cache; `FileTreeModel`/`DiskFileList`/`IThumbnailModel`/`JIListModel`/`ListObservable`/`TreeNode`/`TreeRenderer`/`JIExplorerContext` list a directory for the non-DICOM explorer |
| 14 | i18n fragments, ≥30 % lang pref, native zip | `weasis-core-i18n` fragment (name ends in i18n) Installed @13; language list ≥ 30 %; `weasis-native.zip` via `-P compressXZ` |
| 15 | ViewerHub `/display`, dcm4chee IID | `/display` and `/display/IHEInvokeImageDisplay` 302 `weasis://`; dcm4chee IID templates |

WP-0–4 Have is recorded in `docs/loops/TEAM-B-SCORE-wp0-4.md`. This branch adds Have tests for WP-4 `RotationOp` 0/90/180/270 raster + `ZoomOp` 0/−200/−100/factor + `CropOp` region copy plus `FlipOp`/`WindowOp` VOI/flip (`ImageOpHaveTest`), WP-12 `acquire:patient` `-x`/`-i`/`-s`/`-u` `<tags>` DICOM-keyword XML including nested `IssuerOfAccessionNumberSequence` (`AcquirePatientXmlHaveTest`), WP-0 `weasis:ui` `-q` quit / `-v` window on top (`WeasisUiCommandHaveTest`), WP-13 `dicom:close` `-a`/`-p ID`/`-y`/`-s` removing matching instances from fixture `DicomModel`, WP-1 Help > Keyboard Shortcuts live map (`ShortcutPrefView` / `ShortcutManager`) plus Help > System resources (`ResourceMonitorDialog` heap + native), clone-only `explorer.SeriesFilter` TEXT/DATE/MODALITY (same pref as fixture `main/SeriesFilter`), MX-14 (FoR vs manual), MPR/MIP plus straightened CPR / oblique / curve cross-section plus Shoemake ArcBall plus `VolImageIO`/`RawImageIO` derived slices plus `BuildContext`/`SliceCanvas`/`SegVolumeBuilder`/`MprGeometryModule` plus `SplatContext`/`CrossLineGraphic`, fusion `targetViews` plus PET/CT IPP-Z volume resample plus Hot Iron/PET window LUT plus `FusionMeasurableLayer`, special-SOP factories Active on `lb`, WP-11 Q/R + DICOMweb + STOW + ISO + DICOM Print N-CREATE/N-ACTION + XML/JSON launch manifests plus Q/R import chrome and send C-STORE/STOW chrome plus `AbstractDicomNode`/`DicomNodeListView`/`DicomNodePrefFactory` plus `DicomNodeDialog`/`DicomWebNodeDialog`/`AbstractListEditor`/`HttpHeadersEditor`/`AuthenticationPersistence`/`AuthMethodDialog`/`AuthenticationEditor`, WP-10 SR Content Sequence display in `SRView` plus `SRImageReference`/`SrToolBar`/`EditorPanePrinter` plus ECG Waveform Sequence millivolt leads in `WaveView` plus AU Waveform Sequence PCM play/pause/stop in `AuView` plus RT Structure Set CLOSED_PLANAR contours by Z in `RtDisplayTool` plus SEG overlay `SegmentationTool`/`SegComponentFactory`/`SegPrefView`/`SegRegionLocator`, WP-12 Dicomizer overlay plus still/video/PDF/STL convert Have plus photo-editor rotate/crop/contrast plus calibration mm/pixel plus explorer metadata display/edit/required plus PersonName/AnatomicRegion cell editors plus editor annotate (WP-5 graphics) plus MetadataAction plus AcquireActionButton chrome plus import/central thumbnails plus import grouping plus publish tree/dialog plus browse/series chrome (`BrowsePanel`/`ImportPanel`/`SeriesButton`/`AcquireTabPanel`/`FileSystemDrive`) plus `EditionToolFactory` plus `SeriesGroup`/`AcquireNewSeriesDialog` NAME grouping plus `MediaImporterFactory` still import plus `Global`/`DefaultTaggable` patient tags plus `AcquireImageStatus` TO_PUBLISH vs PUBLISHED plus `AutoLevelListener` histogram stretch and Gogo **17181** smoke, WP-6 Docking Frames **Active**, MigLayout Core+Swing **Active @7**, JAXB-OSGi **Active @7**, WP-5 `XmlGraphicModel` JAXB round-trip plus `PrGraphicUtil` GSPS Graphic Object mapping plus `InterpolatedPath2D`, 2D lens `ZoomWin`, File &gt; Export DICOM (files/ZIP/DICOMDIR), WP-3 `LoadDicom`/`LoadDicomDir` Part-10/DICOMDIR into `DicomModel` plus `LoadDicomObjects` Part-10 list plus `PluginOpeningStrategy` SOP routing plus `DicomViewerPlugin` one-patient tab plus `DicomSeriesHandler`/`HangingProtocols`/`MimeSystemAppFactory`, WP-4 zoom toolbar (−200/−100) and screenshot PNG/JPEG (headless-safe `ScreenshotDialog`) plus rotation 0/90/180/270 chrome plus `ViewCanvas`/`Canvas` plus `ViewerPrefView`/`ViewerPrefFactory` documented W/L and PR overlay tokens plus `DcmHeaderToolBar` dataset tag dump plus `DicomFieldsView` limited vs all plus `AbstractTagSearchPanel`/`TagSearchTablePanel`/`TagSearchDocumentPanel` keyword/hex/value search, WP-9 MX-15 OpenGL 3.3+ / refuse llvmpipe (jogamp **Active @120**, native **Resolved @121**) plus 3D toolbar/LUT/VolumeTool Have, WP-7 MIP `SeriesBuilder` frames, WP-10 `UISegmentationVolumeBuildExecutor` off-EDT `SegVolumeBuilder`, WP-13 `weasis://` parsers plus Gogo `image:get`/`image:close` plus base-explorer `AbstractThumbnailList`/`JIThumbnailListPane` plus `JIThumbnailCache` URI LRU plus `DefaultThumbnailList`/`AThumbnailListPane` plus `ThumbnailList`/`IThumbnailListPane`/`AThumbnailModel` plus `ThumbnailRenderer`/`ThumbnailIcon` plus `FileTreeModel`/`DiskFileList`/`IThumbnailModel`/`JIListModel`/`ListObservable`/`TreeNode`/`TreeRenderer`/`JIExplorerContext`, WP-14 i18n ≥30 % plus `weasis-native.zip`, and WP-15 ViewerHub `/display` plus dcm4chee IID `weasis://`. All **985** Weasis 4.7.3 main Java paths exist; matching a path is **not** feature-complete. Headed §6 Pass stays empty. SCORE may only change Have boxes.
