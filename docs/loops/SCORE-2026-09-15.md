# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| `cursor/wp12-docking-c346` (already merged) | Have for Dicomizer overlay + MX-16 17181 is aligned. Docking jar at 10 was **Installed**, not Active — that hole is this tick. |
| `cursor/wp-13-protocol-c86f` | Protocol idea is right; branch is stale vs WP-11/12. Reimplemented `weasis://` / `$weasis:config` on Weasis launcher `Utils` (fixture path) instead of merging the old tree. `WeasisUri` in `org.weasis.core.api.command` is clone-only on that branch and was not copied. |
| Grok JOGL / Grok surface | No new remote branch to merge this tick (`branchName` still unset). |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–5, 7, 8, 10–12 | keep prior Have | not re-opened |
| 6 | Docking Frames **Active** @10 | FlatLaf + extras shaded into launcher; `com.formdev.flatlaf.extras\|ui\|util` on `system.packages.extra`. Smoke must list Docking Frames Active. Lens / 3D-cursor GUI still not Pass. |
| 9 | unchanged | JOGL natives still missing on `lb` |
| 13 | URI + config + `dicom:*` parsers | No live WADO. `non-dicom-explorer.json` overlay Have. |
| 14–15 | open | i18n / ViewerHub |

Source-surface: filling `FlatWeasisTheme`, `LookAndFeels`, `Utils`, `Option`, `Options` (Weasis 4.7.3 paths). `DicomCommands` is clone-only.

## Forbidden

Did not tick CHECKLIST §6 Pass.
