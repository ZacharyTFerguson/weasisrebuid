# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior `image:get`/`image:close` (`af10e06`) | Kept. POSIX `-f`/`-u`/`-a`/`-g`/`-s` match COMMANDS.md; local ImageIO `MediaReader`; remote `-u` does not fetch; Gogo smoke still prints `image:get Have`. |
| `origin/cursor/wp-14-*`, `wp-15-*`, `wp-13-*`, `*-3711` | Stale or a different experiment; **not merged.** |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–3, 5–8, 10–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; Base 2D Viewer Active @100; `image:get` Have; Docking Frames Active @10; MigLayout+JAXB-OSGi Active @7 |
| 4 | Rotation chrome | `RotationToolBar` 0/90/180/270 applies `DefaultView2d.setRotation`. Zoom/screenshot Have kept. |
| 9 | 3D toolbar/LUT/VolumeTool | COMPOSITE/MIP/MINIP/ISO and CT Soft Tissue/Bone presets apply to `EventManager.getSelectedView()` only when MX-15 allows VR. llvmpipe/no-GPU stay COMPOSITE. `ExternalView3DToolbar` opens `View3DContainer`. jogamp Active @120. |
| layout | 985/985 | Matching a path is not feature-complete. Remaining: graphics JAXB annotations, many core UI stubs. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, Base 2D Viewer Active @100, `image:get` Have, Docking Frames Active @10, MigLayout+JAXB Active @7, viewer3d Active @120); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not merge stale sibling branches. Did not treat layout Have as Weasis-complete.
