# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| `cursor/wp12-docking-c346` (already merged) | Dicomizer overlay + MX-16 17181 aligned. Docking at 10 was Installed until FlatLaf extras this tick. |
| `cursor/wp-13-protocol-c86f` | Protocol idea is right; branch is stale vs WP-11/12. Reimplemented `weasis://` / `$weasis:config` on launcher `Utils` (Weasis path). |
| [Grok surface types](bc-e2a69150-a105-5fa6-b7ce-30599dafa901) `4720fc8` | **985/985** fixture paths present, **0 unexpected**. Many types were empty public classes (example: `DicomDirImport`). Layout Have, **not** feature-complete Weasis. Follow-up: `DicomDirImport` / `DicomDirLoader` now delegate to `LoadLocalDicom`. Remaining empty types still exist. |
| [Grok JOGL 3D WP-9](bc-5448d97a-ac85-525b-ae67-567198f6216b) `cursor/wp9-jogl-3d-216b` | Merged. MX-15 JUnit + `lb`: jogamp **Active @120**, viewer3d **Active @120**, native **Resolved @121**. Docking Frames stayed **Active @10**. Headed 3D tutorial is not Pass. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–5, 7, 8, 10–13 | keep prior Have | not re-opened |
| 6 | Docking Frames **Active** @10 | Verified `lb`: Docking Frames 1.1.7 Active. Codec/explorer/viewer2d Active. Gogo **17179**. Lens / 3D-cursor GUI is not Pass. |
| 9 | MX-15 + JOGL | GL 3.3+; refuse llvmpipe; N/A no GPU. Smoke: JOGL Active @120, viewer3d Active @120, native Resolved @121. |
| 14 | i18n ≥30 % + native zip | Language list hides locales under 30 % (`messages_it` 20 % hidden, `messages_fr` 40 % shown). `weasis-core-i18n` fragment **Resolved @13**. `mvn -P compressXZ -f weasis-distributions package` writes `weasis-native.zip` (launcher + etc/config + bin, not an m2 dump). |
| 15 | open | ViewerHub / IID |

Source-surface: present **985/985**, clone-only 76, unexpected 0, missing **0**. Matching a path is not feature-complete.

Gates: `mvn test` PASS, `spotless:check` PASS, `gogo-smoke` SMOKE_OK, `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass.
