# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior ViewerToolBar / cine / stills ImportToolBar (`4072fb3`) | Kept. `sequence` still scrolls; cine still ticks frames; ImportToolBar still uses ImageIO. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. |
| This tick MeasureToolBar + mouse draw | Independent Have: toolbar has documented tool buttons; click-drag-release builds a complete `LineGraphic`; angle needs a second click for the third handle. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–4, 6–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; JAXB-OSGi Active @7; Docking Frames Active @10; cine/ViewerToolBar/stills import unchanged |
| 5 | MeasureToolBar chrome + draw | Seven type buttons (distance/angle/polyline/textbox/rectangle/ellipse/polygon); left action `measure`; click-drag-release distance length 5; angle 90° after second click |
| layout | 985/985 | Matching a path is not feature-complete. Remaining: base `EventManager`/`InfoLayer`/`ImageTool`/`DisplayTool` empty; polyline still needs a double-click Pass. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, weasis-core Active @12, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
