# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior navigation / explorer thumbs / GraphicsPane (`49c9877`) | Kept. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. |
| This tick docking / MPR keys / explorer panes / Ctrl-drag | Independent Have from SHORTCUTS.md (weasis.org 4.7 shortcuts + MPR tutorial). |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–2, 5, 8–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; JAXB-OSGi Active @7; docking-frames Active @10 |
| 3 | PatientPane / StudyPane | `PatientSelectionManager` groups `DicomModel` patient → study → series; explorer list follows the selected study |
| 4 | Ctrl-drag accelerate | Ctrl ×2, Ctrl+Shift ×4; rubber-band/draw not accelerated; Ctrl/Alt do not steal letter mouse-actions except Alt+P |
| 6 | central-panel keys | Ctrl+Tab / Ctrl+Shift+Tab cycle; Ctrl+M maximize/restore; Ctrl+W close; Ctrl+E externalize; Ctrl+N normalize; Ctrl+Shift+E docking list |
| 7 | MPR shortcuts | Alt+X/C/V center / show-center / show-crosshair on selected plane; Ctrl+Alt variants for all planes; Ctrl+Alt+B cycles MIP None/Min/Mean/Max; Alt+wheel thickness on selected axis |
| layout | 985/985 | Matching a path is not feature-complete. Remaining thin types include many acquire stubs. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, docking-frames Active @10, weasis-core Active @12, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
