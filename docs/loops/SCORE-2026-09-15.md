# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior docking / MPR keys / explorer panes / Ctrl-drag (`9944565`) | Kept. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. |
| This tick 2D chrome + remaining weasis.org 4.7 keys | Independent Have from Shortcuts + MPR tutorial + empty Lut/Reset/Basic3D fixtures. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–3, 5–6, 8–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; JAXB-OSGi Active @7; docking-frames Active @10 |
| 4 | remaining 2D keys + LUT/reset chrome | Alt+R/L 90°; Alt+F flip; Ctrl+Space next left action; Ctrl+/- zoom; Ctrl+Enter reset zoom; Tab/Shift+Tab layout views; `LutToolBar` Gray/Sine/HotIron + Inverse; `ResetTools` applies `dcmview2d:reset` tokens |
| 7 | Basic3DToolBar | Opens `MprContainer` without stealing `image/dicom` |
| layout | 985/985 | Matching a path is not feature-complete. Remaining thin types include many acquire, wave, SR, and RT stubs. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, docking-frames Active @10, weasis-core Active @12, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
