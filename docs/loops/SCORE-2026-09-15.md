# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior acquire explorer thumbnails / import / publish (`d96c610`) | Kept. Import/central thumbnail, grouping, and publish Have tests still green in this tick’s `mvn test`. |
| Prior acquire calibration + metadata (`825beb0`) | Kept. Calibration mm/pixel and metadata table Have tests still green. |
| Prior acquire editor + ArcBall (`9e8739e`) | Kept. Acquire/ArcBall Have tests still green. |
| Prior MPR `VolImageIO` / `RawImageIO` (`2da525e`) | Kept. Slice IO Have tests still green; this tick feeds them from `BuildContext` / `SliceCanvas`. |
| Prior PersonName/AnatomicRegion editors + annotate / metadata / action chrome (`2b973be`) | Kept. PersonName, AnatomicRegion, annotate, metadata gate, and action/submit chrome Have tests still green. |
| Prior acquire browse/series chrome + `EditionToolFactory` (`fb75066`) | Kept. FileSystemDrive, BrowsePanel, ImportPanel, SeriesButton/AcquireTabPanel, and EditionToolFactory Have tests still green. |
| Prior acquire SeriesGroup / importer / patient tags / auto-level (`89d27e8`) | Kept. SeriesGroup, NAME dialog, MediaImporterFactory, Global/DefaultTaggable, AcquireImageStatus, and AutoLevelListener Have tests still green. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. WP-7 sibling invents `CprBuilder`/`Mip2d`; other `*-3711` branches invent `AuPlayer` (not in the 4.7.3 inventory). |
| This tick MPR slice canvas / SEG overlay / ViewCanvas | Independent Have: `BuildContext` holds Volume + `MprAxis` + index + MIP for `VolImageIO`; `SliceCanvas` paints that `BufferedImage`; `SegVolumeBuilder` rasterizes SEG frames into `VolumeByte`/`VolumeShort`; `MprGeometryModule` round-trips plane geometry; `ViewCanvas`/`Canvas` are the core types `DefaultView2d` and `SliceCanvas` implement; `SegmentationTool` binds overlay visibility; `SegComponentFactory` attaches `SegGraphic` overlays; `SegPrefView` persists opacity/fill/contour. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–6, 8–9, 11, 13–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; docking-frames Active @10; DICOM Audio Player Active @75 |
| 4 | keep prior 2D Have; add `ViewCanvas`/`Canvas` | `DefaultView2d` implements `ViewCanvas`; affine + SEG visibility live on the canvas type MPR/2D share |
| 7 | keep VolImageIO / RawImageIO; add SliceCanvas / BuildContext / SegVolumeBuilder / MprGeometryModule | `SliceCanvas` paints `VolImageIO` pixels; MIP thickness via `BuildContext`; SEG frames rasterize to `VolumeByte`/`VolumeShort`; PR geometry origin/row/column |
| 10 | keep SR/ECG/AU/RT; add SEG overlay chrome | `SegmentationTool` dockable; `SegComponentFactory` mask overlays; `SegPrefView` opacity/fill/contour; Alt+S already Have in WP-4 |
| 12 | keep prior editor/meta/publish/browse/SeriesGroup | Not reworked this tick |
| layout | 985/985 | Matching a path is not feature-complete. Remaining empty stubs include `DicomExplorer`/`LoadDicom`/`LoadDicomDir`/`DicomSeriesHandler`/`HangingProtocols`/`MimeSystemAppFactory`, `ViewerPrefView`, MPR `SplatContext`/`CrossLineGraphic`, `SegRegionLocator`, `SeriesBuilder`, `UISegmentationVolumeBuildExecutor`, and base-explorer thumbnail list types. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green locally; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, docking-frames Active @10, weasis-core Active @12, DICOM Audio Player Active @75, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`. Origin CI still fails on pre-existing headless `ScreenshotDialog` (`ZoomScreenshotHaveTest`); not a regression of this tick’s MPR/SEG Have.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
