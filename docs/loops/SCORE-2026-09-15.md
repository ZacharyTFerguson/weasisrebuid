# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| This tick `weasis-native.zip` via `-P compressXZ` Have (`67a8913`) | Portable tree (`weasis-launcher.jar` + `etc/config` + `bin/Dicomizer`); production SNAPSHOT refused; `weasis-distributions` stays out of the root reactor. Hang 1×2 / layout `-n` / fill / LUT Sequence / remaining-tab `CLocation` / filter / Help / KO / Dummy / `dicom:close` / `weasis:ui` / `acquire:patient` XML / image-op process / i18n fragments @13 not touched. Empty braces gone ≠ Pass. |
| Prior remaining Messages-host i18n fragments Have (`faefc87`) | Kept. 18 fragments at `felix.auto.install.13` (install not start). |
| Prior explorer / viewer2d i18n fragments Have (`defea69`) | Kept. `weasis-dicom-explorer-i18n` / `weasis-dicom-viewer2d-i18n` Fragment-Host explorer / viewer2d. |
| Prior `FilterOp` / `PseudoColorOp` / `ShutterOp` / `OverlayOp` process Have (`9ff0336`) | Kept. `FilterOp` None passthrough / Sharpen convolve; `PseudoColorOp` Gray identity / Inverse `255-v`; `ShutterOp` blacks outside enabled box; `OverlayOp` ORs 60xx bits onto 8-bit grey. |
| Prior `BrightnessOp` / `AutoLevelsOp` / `MaskOp` / `MergeImgOp` process Have (`84175e4`) | Kept. `BrightnessOp` contrast/brightness rescale (identity passthrough); `AutoLevelsOp` min/max luma stretch to 8-bit; `MaskOp` luma-0 hide; `MergeImgOp` overlay copy. |
| Prior `RotationOp` / `ZoomOp` / `CropOp` process Have (`da8a30a`) | Kept. `RotationOp` 0/90/180/270 raster (0 passthrough); `ZoomOp` 0 default / −200 best fit / −100 real size / positive scale; `CropOp` intersected region copy (does not share raster). |
| Prior `FlipOp` / `WindowOp` process Have (`13bc914`) | Kept. `FlipOp` mirrors `BufferedImage` when `P_HORIZONTAL`; `WindowOp`/`WindowAndPresetsOp` apply linear/SIGMOID VOI; Alt+F paint uses scale −x. |
| Prior `acquire:patient` `<tags>` XML context depth Have (`99bb097`) | Kept. COMMANDS.md `-x`/`-i`/`-s`/`-u` parse DICOM-keyword `<tags>` including nested issuer sequence onto album `Global`. |
| Prior `weasis:ui` `-q` quit / `-v` window on top Have (`9cf3b49`) | Kept. COMMANDS.md `-q` stops system bundle (null-safe when not activated); `-v` un-iconifies, shows, and `toFront`s `UICore` application window. |
| Prior `dicom:close` removes from fixture `DicomModel` (`4551c21`) | Kept. COMMANDS.md `-a` / `-p ID` / `-y` / `-s` delete matching instances. Token `close-all` kept. |
| Prior clone-only `explorer.SeriesFilter` TEXT/DATE/MODALITY (`e165acd`) | Kept. Delegates to fixture `main/SeriesFilter`. |
| Prior Help > System resources (`0d81b8a`) | Kept. SRS Help > System resources diagnostic via `ResourceMonitorDialog`. Keyboard Shortcuts / About / Licenses order kept. |
| Prior Help > Keyboard Shortcuts live map (`bb06eb8`) | Kept. SHORTCUTS.md live map (`ShortcutPrefView` / `ShortcutManager` defaults; no overlay key). About/Licenses kept. |
| Prior explorer TEXT/DATE/MODALITY filter (`b077598`) | Kept. Fixture `main/SeriesFilter` honors `weasis.dicom.explorer.filter.mode` TEXT/DATE/MODALITY. `explorer-series` JList name kept. |
| Prior remaining viewer-tab drag-split (`7fa1de6`) | Kept. Real Docking Frames `CLocation.working()` east/west/south/north. First tab stays. Hang 1×2 is not this split. |
| Prior VOI LUT Sequence + SIGMOID (`c3de73b`) | Kept. `LutPipeline.applyVoiLut` / `applyVoiSigmoid`; digit keys still apply WC/WW then Sequence items. Dummy off. Hang 1×2 / layout `-n` / explorer-row fill not touched. |
| Prior arm explorer series-row press (`0b62ce6`) | Composer headed PASS: 2×2 chest\|knee empty bottoms; after drop chest in bottom-left, one DICOM 2D tab, bottom-right empty. Logs `beginDrag` + `hangAtScreen`. Keep. |
| Prior arm `dragging()` on left thumbnail press (`77ea3af` / `1d216f6` / SCORE `e4cdb49`) | Kept for viewer thumbs. Not enough headed: Composer does not press `SeriesThumbnail`. |
| Prior toolkit `MOUSE_RELEASED` hang (`9da7fd0` / SCORE `b10fa81`) | Superseded for the headed miss: listener ran only while `dragging()!=null`, and `dragging()` was never set. |
| Prior hang extra View2d from dragDropEnd + view-grid (`d07e366` / SCORE `f8212d0`) | Superseded for the headed miss: `dragDropEnd`/`lastOver` never hangCell on `f8212d0`. View-grid mapping and `copyPaint` kept. |
| Prior hang empty extra View2d under pointer (`843904a` / SCORE `a29236a`) | Superseded for the headed miss: `mouseReleased` / `hangAtPointer` MouseInfo + View2d screen boxes did not hangCell on `a29236a`. |
| Prior restore View 2×2 / `layout -n` extra empty cells (`1357328` / `aee2dcf` / SCORE `2b847c2`) | Kept. Composer headed PASS empty bottoms on `2b847c2`/`a29236a`. Skip W/L while `dragging()!=null`. |
| Prior native drop fills empty 2×2 cell (`bbb7de5` / SCORE `de45a67`) | Superseded for the headed miss: `lastDragged` / `dropCellAtScreen` only help if drop/`importAt` runs. Native `SeriesDrop.drop` is not delivered; fill is now `hangAtScreen` on release/exportDone. |
| Prior hang 1×2 stays one tab (`6d71feb` / SCORE `0ae82da`) | Kept. Composer headed PASS chest left / knee right one tab on `0ae82da`. |
| Prior explorer thumbnail drag start (`cb06add` / SCORE `cf8c34c`) | Kept. BUTTON1 + `DragSource` threshold + `withLeft` + retry if `dragging()==null`. `SeriesSelection` `stringFlavor`. |
| Prior CWorkingArea remaining-series east split (`97c4708` / SCORE `8cc1e3c`) | Kept **without** auto-`show()` of every plugin. `viewer-tabs` fill `viewer-work`; `splitSeries` still `CLocation.working().east(0.5)`. |
| Prior headed explorer series drop onto THAT empty View2d (`113ba2f` / SCORE `7f1b403`) | Kept. `dropCellAt` / `canImport` while dragging / glass+CContentArea handlers not regressed. Composer still owes headed proof after relaunch (old `7f1b403` jar had no drag start). |
| Prior per-series Docking Frames float (`53fda50` / SCORE `1a54260`) | Kept. Ctrl+E still floats the focused series dockable; Ctrl+N restores into the working area. Explorer dock still uncloseable. |
| Prior Swing series drop onto empty hang cell (`3a73a7c` / SCORE `027ef17`) | Superseded for the headed miss: paste `TransferSupport` was not a native drop. Headed path now steals the docking drop target and hit-tests the cell. `ThumbDrag` still one `exportAsDrag` per press. |
| Prior focused-tab SeriesViewerUI toolbar rebind (`6cc8ce5` / SCORE `26aea8b`) | Kept. Tab change still rebinds window bars from `SeriesViewerUI`; Import JButton stays. |
| Prior explorer series DnD onto viewer (`fd34dec` / SCORE `203b111`) | Superseded for the headed miss: Have was not a headed drop. Swing import now fills the empty hang cell. |
| Prior layout `-n` extra cells empty (`675fe44` / SCORE `ddf6fbb`) | Kept. `dcmview2d:layout -n 4` extra cells stay **empty**, not clones of the primary series. Hang 1×2 two DX stay different Series UIDs after `-n 4`. |
| Prior KO apply-on-open (`8456a58` / SCORE `637348d`) | Kept. Overlay open still creates **no** extra tab. `View2dContainer.applyOverlay` still applies `KOSpecialElement` via `KOManager`; DX 1×2 extra hang cell stays empty. |
| Prior Dicomizer Gogo 17181 (`4233c70` / SCORE `3a89729`) | Kept. Documented `$weasis:config … pro="gosh.port 17181"` plus overlay profile. Smoke binds **17181** (`DICOMIZER_SMOKE_OK`); desktop `gogo-smoke` stays **17179** `SMOKE_OK`. JSON still has no `gosh.port`. |
| Prior i18n ≥30% of documented Messages bundles (`2372eb5` / SCORE `34e86b1`) | Kept. File &gt; Preferences language list still uses the 18 fixture `Messages.java` bundle names; FR listed, IT hidden. |
| Prior MPR/3D title-only fixture prefs (`f8b6aec` / SCORE `b22ed4d`) | Kept. `MprPrefView` still title-only; `Viewer3dPrefView` still persists `weasis.force.3d` default false. |
| Prior headed Monitors pitch OK persist wiring (`45d07df` / SCORE `4a5940f`) | Kept. OK still commits open spinners; `ScreenPrefView` still reads Locale.US editor text; factory reload still **0.35**. MX-07 still not session Manual Calibration. |
| Prior P0 Import DICOM close/dispose (`8d7df42` / SCORE `a5a46e0`) | Kept. DOCUMENT_MODAL + WeasisWin DO_NOTHING while an owned dialog is showing. Composer already PASS Import-close + hang 1×2 on `92c3c70`. |
| Prior `weasis://` URI → Gogo dispatch (`2d7f287` / SCORE `92c3c70`) | Kept. `WeasisUriHaveTest` still green. |
| Prior Monitors pitch Preferences OK store/reload (`817b299` / SCORE `c76d2e3`) | Kept. JSpinner still `commitEdit`s; OK still writes `weasis.monitor.pitch`; reload still **0.35**. MX-07 still not session Manual Calibration. |
| Prior Draw / Graphic / Labels / Shortcuts prefs (`1c5e04d` / SCORE `28766e9`) | Kept. Draw nests Graphic + Labels; Shortcuts T/W/C/M (cine not clipboard) still green. |
| Prior DummyViewerPlugin off-startup + dummy-MIME series (`540e339` / SCORE `c8ca4a9`) | Kept. Factory still reads only `image/dummy` at level 1000; `WeasisWin` starts with **0** viewer tabs. |
| Prior title-only Logging / Launchers / Monitors prefs (`4440051` / SCORE `b346796`) | Kept. Documented felix/sling log keys, Import/Q/R/Export/Send visibility, and MX-07 monitor pitch Have still green. |
| Prior hanging extra cells empty (`ff7640d` / SCORE `22340b9`) | Kept. `applyHanging` still grows empty extra cells; chest then knee one 1×2, different Series UIDs. |
| Prior headed hanging slot reuse (`28ea8a0` / SCORE `f1304db`) | Kept. Hang-slot reuse across `new PluginOpeningStrategy` and different patients still green. |
| Prior FileModel / DefaultGraphicLabel / KO rejection (`c4dba61` / SCORE `fe0a25f`) | Kept. File tree, label paint, and 113001 rejection Have still green. |
| Prior hanging protocol on `View2dContainer` (`ed033fa` / SCORE `b5251be`) | Kept. DX 1×2 / MG 2×2 / CT 1×1 and different series in extra cells still green. |
| Prior KO star/filter on series frames (`5508fa8` / SCORE `b1cdf30`) | Kept. Filter still restricts `View2d.visibleMedias()` to starred SOPs; KO document refs still import + filter on. |
| Prior remaining editor.image empty braces (`7a6b0fe` / SCORE `dd86d85`) | Kept. Sequence/focus/popup/transfer/LUT/FoR handlers still green. |
| Prior ImageStatistics / ConvexHull / AdvancedShape / ReferencedSeries (`249d6fb` / SCORE `365545d`) | Kept. Hull/MER/ROI stats/SOP refs still green. |
| Prior ViewButton / PlayViewButton / AreaIcon / TabPlacement (`f28f4bd` / SCORE `6ee3fff`) | Kept. Overlay Play/Stop toggles cine; `AreaIcon` paints the fill; `WeasisWin` viewer tabs stay **TOP**. |
| Prior graphic UUID/layer/measure/clipboard/drag (`3700dc6` / SCORE `82803a4`) | Kept. UUID, IMAGE layer, MeasureItems, clipboard, drag sequences still green. |
| Prior SplitSeriesManager / JIUtility / DicomModelQueryResult (`532d667` / SCORE `d41ea45`) | Kept. MR echo split, non-DICOM explorer helpers, WADO/MF manifest apply still green. |
| Prior explorer KO/PR/SEG/RT corner icons (`a85d840` / SCORE `ff1b05e`) | Kept. Explorer thumbnails still stamp overlay MIME: yellow corner **KO** / **PR** / **SEG** / **RT**. |
| Prior Edit/Tools/Window menus (`aaf4e87` / SCORE `415fab1`) | Kept. File/Edit/View/Tools/Window/Help Have still green. |
| Prior measure/draw persist in image space (`37f9f1d` / SCORE `9afe1a4`) | Kept. `MeasureToolBarHaveTest.distanceStaysOnImagePixelsAfterZoomAndStillPaints` still green. |
| Prior overlay W: half-window (`6f96dcc` / SCORE `85de1cf`) | Kept. Headed VOI 0/1 PASS on `85de1cf` (`W:65535 → 32767 → 65535`). |
| Prior key 0 data-range + window digit bindings (`191a5d8` / SCORE `1005aec`) | Kept. Digit dispatcher and root-pane bindings still present. |
| Prior focused docking plugin keys + layout (`8a0d96a` / SCORE `de250a3`) | Kept. Dispatcher still present; Composer headed `de250a3` 1/5 looked like no-ops because of the single WC/WW, not because 2×2 failed. |
| Prior real 1×2 / 2×2 split (`3a4f5a4` / SCORE `8993cca`) | Kept. GridLayout Have still green; Composer headed `afba495` lacked this jar. |
| Prior acquire explorer thumbnails / import / publish (`d96c610`) | Kept. Import/central thumbnail, grouping, and publish Have tests still green. |
| Prior acquire calibration + metadata (`825beb0`) | Kept. Calibration mm/pixel and metadata table Have tests still green. |
| Prior acquire editor + ArcBall (`9e8739e`) | Kept. Acquire/ArcBall Have tests still green. |
| Prior MPR `VolImageIO` / `RawImageIO` (`2da525e`) | Kept. Slice IO Have tests still green. |
| Prior PersonName/AnatomicRegion editors + annotate / metadata / action chrome (`2b973be`) | Kept. PersonName, AnatomicRegion, annotate, metadata gate, and action/submit chrome Have tests still green. |
| Prior acquire browse/series chrome + `EditionToolFactory` (`fb75066`) | Kept. FileSystemDrive, BrowsePanel, ImportPanel, SeriesButton/AcquireTabPanel, and EditionToolFactory Have tests still green. |
| Prior acquire SeriesGroup / importer / patient tags / auto-level (`89d27e8`) | Kept. SeriesGroup, NAME dialog, MediaImporterFactory, Global/DefaultTaggable, AcquireImageStatus, and AutoLevelListener Have tests still green. |
| Prior MPR SliceCanvas / SEG overlay / ViewCanvas (`1f0f0c9`) | Kept. BuildContext, SliceCanvas, SegVolumeBuilder, MprGeometryModule, SegmentationTool, SegComponentFactory, and SegPrefView Have tests still green. |
| Prior explorer loaders / MIME system app / headless screenshot (`74362d5`) | Kept. LoadDicom/LoadDicomDir/DicomSeriesHandler/HangingProtocols/MimeSystemAppFactory and ScreenshotDialog Have tests still green. |
| Prior MPR splat / cross lines / SEG locator / base-explorer thumbnails (`9d67c12`) | Kept. SplatContext, CrossLineGraphic, SegRegionLocator, AbstractThumbnailList/JIThumbnailListPane Have tests still green. |
| Prior explorer open + thumbnail URI cache (`0cc5bab`) | Kept. LoadDicomObjects, PluginOpeningStrategy, DicomViewerPlugin, JIThumbnailCache, DefaultThumbnailList, and AThumbnailListPane Have tests still green. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. WP-7 sibling invents `CprBuilder`/`Mip2d`; other `*-3711` branches invent `AuPlayer` (not in the 4.7.3 inventory). |
| Prior 2D prefs / MIP series / SEG volume executor / thumbnail renderer (`f7d186b`) | Kept. ViewerPref, MIP SeriesBuilder, SEG volume executor, and thumbnail renderer Have tests still green. |
| Prior DICOM node prefs / header dump / SR IMAGE refs / file-tree (`8a4bed8`) | Kept. AbstractDicomNode `DCM4CHEE:11112`, DicomNodeListView, DcmHeaderToolBar, SR IMAGE refs, file-tree, and FusionMeasurableLayer Have tests still green. |
| Prior DICOM node dialogs / list editor / auth+headers (`a6c064c`) | Kept. DicomNodeDialog / DicomWebNodeDialog / AbstractListEditor / HttpHeadersEditor / AuthenticationPersistence Have tests still green. |
| Prior DICOM fields / tag search / PR graphics (`5d31e89`) | Kept. DicomFieldsView limited vs all, tag search panels, and PrGraphicUtil/InterpolatedPath2D Have tests still green. |
| Prior Import→DICOM explorer+viewer chrome (`d6a853f`) | Kept. Shared `LocalPersistence` model, explorer thumbnails, `WeasisWin` chrome, and FlatLaf bootdelegation Have tests still green. |
| Prior unsigned 8-bit W/L + no dummy at startup (`11d1f66`) | Kept. `WindowLevelPaintTest.unsigned8BitDxBoneIsBrighterThanSoftTissue`; dummy not attached at startup. |
| Sibling LoadDicom explorer series Have (`a84038d`) | Kept. `LoadDicomExplorerHaveTest` still green; not clobbered. |
| Prior Gogo/Import shared-model GUI bind (`494b33f` / SCORE `9b3a681`) | Kept. `DicomCommandsTest.getLocalLoadsSharedModel` and `ImportExplorerHaveTest` still green. |
| Prior tab host + View menu + explorer combos (`d76b96a`) | Kept. `WeasisWinChromeHaveTest.secondPluginAddsATabInsteadOfReplacingCenter` and nested `viewer-tabs` Have still green. |
| Prior Docking Frames central host (`4b11d91`) | Kept. `WeasisWinChromeHaveTest.dockingHostPutsExplorerLeftOfNamedViewerTabs` still green. |
| Prior VOI presets 0–9 (`43aae05`) | Kept. `View2dPresetSegHaveTest` digit-key Have still green. |
| Prior jsvg shade + bootdelegation (`afba495`) | Kept. Launcher shade/bootdelegation not touched this tick. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 14 | `weasis-native.zip` via `-P compressXZ` portable tree; production version must not be SNAPSHOT | `NativeDistributionHaveTest.compressXzProfileWritesPortableWeasisNativeZip`; `productionVersionMustNotBeSnapshot`. Dummy off. |
| 14 | keep remaining documented Messages-host i18n fragments Installed @13 (18 jars, keep core/explorer/viewer2d) | `I18nStartLevelTest.shippingCatalogInstallsHostI18nFragmentsAt13` 18 files; host HaveTests EN/FR; fragment tests name ends i18n + Fragment-Host + DE on classpath. Dummy off. |
| 14 | keep `weasis-dicom-explorer-i18n` + `weasis-dicom-viewer2d-i18n` fragments Installed @13 (plus keep `weasis-core-i18n`) | `ExplorerI18nFragmentTest` 2/2; `Viewer2dI18nFragmentTest` 2/2; `ExplorerMessagesHaveTest` 1/1; `Viewer2dMessagesHaveTest` 1/1. Dummy off. |
| 4 | keep `FilterOp` None/Sharpen + `PseudoColorOp` Gray/Inverse + `ShutterOp` black-outside + `OverlayOp` 60xx bits | `ImageOpHaveTest.filterNonePassthroughAndSharpenRaisesCenter`; `pseudoColorGrayPassthroughAndInverseMapsComplement`; `shutterBlacksOutsideEnabledBox`; `overlayBitsOrOntoGrayWithoutSharingRaster`. Dummy off. |
| 4 | keep `BrightnessOp` contrast/brightness rescale + `AutoLevelsOp` histogram stretch + `MaskOp` luma-0 hide + `MergeImgOp` overlay | `ImageOpHaveTest.brightnessIdentityPassthroughAndRescaleMapsGray`; `autoLevelsStretchesMinMaxToFullRange`; `maskZerosBlackenSourcePixels`; `mergeOverlayDrawsOnTopWithoutSharingRaster`. Dummy off. |
| 4 | keep `RotationOp` 0/90/180/270 raster + `ZoomOp` 0/−200/−100/factor + `CropOp` region copy | `ImageOpHaveTest.rotation90CwPutsLeftPixelOnTop`; `zoomZeroAndRealSizePassthroughAndBestFitScales`; `cropRegionCopiesPixelsWithoutSharingRaster`. Dummy off. |
| 4 | keep `FlipOp` horizontal flip + `WindowOp` linear/SIGMOID VOI on `BufferedImage` | `ImageOpHaveTest.flipHorizontalMirrorsBufferedImage`; `linearWindowMapsLevelToMidGrayAndSigmoidDiffersAtUpper`. Dummy off. |
| 12 | keep `acquire:patient` `-x`/`-i`/`-s`/`-u` `<tags>` DICOM-keyword XML including nested issuer sequence | `AcquirePatientXmlHaveTest.documentedTagsXmlFillsGlobalIncludingNestedIssuer`; flags still `-x`/`-i`/`-s`/`-u`. Dummy off. |
| 0 | keep `weasis:ui` `-q` quit / `-v` window on top | `WeasisUiCommandHaveTest.quitStopsSystemBundleWhenActivated`; `quitWithoutActivateStillReturnsStopping`; `visiblePutsApplicationWindowOnTop`. Dummy off. |
| 13 | keep `dicom:close` `-a` / `-p ID` / `-y` study UID / `-s` series UID removes matching instances from fixture `DicomModel` | `DicomCommandsTest.closeAll`; `closePatientStudySeriesRemovesOnlyMatching`. Token `close-all` kept. Dummy off. |
| 3 | keep clone-only `explorer.SeriesFilter` TEXT / DATE / MODALITY | `SeriesFilterTest` |
| 0–1 | keep Help > System resources diagnostic (heap + native image memory) | `WeasisWinChromeHaveTest.helpSystemResourcesShowsHeapAndNativeDiagnostic` |
| 0–1 | keep Help > Keyboard Shortcuts live map | `WeasisWinChromeHaveTest.helpKeyboardShortcutsShowsLiveMapFromShortcutManager` |
| 3 | keep explorer `weasis.dicom.explorer.filter.mode` TEXT / DATE / MODALITY on fixture `main/SeriesFilter` | `SeriesFilterHaveTest`; `explorer-series` JList kept. |
| 6 | keep remaining viewer-tab drag onto `viewer-work` edge is a real `CLocation.working()` split (east/west/south/north) | `WeasisWinChromeHaveTest.viewerTabDragToEastEdgeSplitsWorkingArea`; hang 1×2 is not east-split. |
| 4 | keep VOI LUT Sequence tables + SIGMOID function beyond linear WC/WW | `LutPipeline.applyVoiLut` / `applyVoiSigmoid`; `DicomMediaUtils.voiPresets` appends Sequence items; `WindowLevelPainter` paints table; `View2d` digit keys apply LUT presets; `View2dLutPaintTest.sequenceItemPresetPaintsLutTableNotWindowCenter`. Dummy off. |
| 3 | keep explorer series-row **and** `SeriesThumbnail` left-press arms `dragging()`; AWT release fills THAT empty 2×2 `View2d` | Composer headed PASS on `0b62ce6`. No native `exportAsDrag`. Hang 1×2 / layout `-n` kept. |
| 4 | keep View 2×2 and `dcmview2d:layout -n 4` grow empty extra cells after hang 1×2 | Composer headed PASS empty bottoms on `2b847c2`; extras stay empty until drop |
| 3 | keep explorer series drag start + `lastDragged` + skip W/L while `dragging()!=null` | `ThumbDrag` BUTTON1 + threshold; `ImageViewerEventManager` skip while dragging |
| 5 | keep hang 1×2 two DX one tab (chest left, knee right); not east-split | `HangingProtocolOpenHaveTest.chestThenKneeOnWeasisWinStaysOneTabNotEastSplit`; Composer headed PASS on `0ae82da` |
| 6 | keep remaining-series `splitSeries` east (explicit, not hang cells); tab drag uses the same `CLocation` surface | `WeasisWinChromeHaveTest.remainingSeriesTabsSplitInWorkingArea`; first tab stays; second dock east 0.5 |
| 3 | keep explorer series drag start (left press arms `dragging()`, no native `exportAsDrag`) | `DicomExplorer` series-row + `SeriesPane.ThumbDrag` `beginDrag` on press; skip W/L while `dragging()!=null` |
| 6 | keep per-series Docking Frames float (Ctrl+E / Ctrl+N) | `WeasisWinChromeHaveTest.externalizeSelectedPluginAddsPerSeriesDockable`; explorer dock still uncloseable |
| 0–1 / 4 | keep focused-tab SeriesViewerUI toolbar rebind (Import JButton kept) | `SeriesViewerUiHaveTest` still green |
| 4 | keep layout `-n` extra cells empty (not clone-only); hang 1×2 two DX stay different | `LayoutNEmptyCellsHaveTest`; `HangingProtocolContainerHaveTest` still green |
| 3 / 5 | keep KO apply-on-open: overlay, no tab, hang extra empty, filter on starred SOPs | `KoApplyOnOpenHaveTest`; explorer KO overlay hang-slot Have |
| 12 | keep Dicomizer Gogo **17181**; desktop stays **17179** | `DicomizerGogoHaveTest`; `scripts/dicomizer-gogo-smoke.sh` `DICOMIZER_SMOKE_OK`. JSON has no gosh.port. |
| 14 | keep listed languages ≥30% of documented Messages bundles | `DocumentedBundlesI18nHaveTest` still green. |
| 7 / 9 | keep MPR title-only fixture + 3D `weasis.force.3d` persist | `MprPrefHaveTest` / `Viewer3dPrefHaveTest` still green. |
| 4 / MX-07 | keep Prefs OK stores typed Monitors pitch; factory reload shows 0.35 | `TitlePrefHaveTest.preferenceOkStoresTypedPitchAndReloadShowsSameValue` still clicks OK. |
| 3 | keep Import close leaves WeasisWin up | DOCUMENT_MODAL + DO_NOTHING while owned dialog showing |
| 13 | keep `weasis://` Gogo dispatch | `WeasisUriHaveTest` still green |
| 0–1 / 5 | keep Draw / Graphic / Labels / Shortcuts | `DrawPrefHaveTest` still green |
| 1 | keep Dummy off startup | `DummyHaveTest` still green |
| 0–1 | keep Logging / Launchers / Monitors prefs | `TitlePrefHaveTest` still green |
| 5 | keep headed DX hanging one 1×2, extra cell empty | Two DX stay different Series UIDs after layout `-n 4`; extra 2×2 cells stay empty. |
| 5 | keep FileModel / labels / KO rejection | Not touched. |
| 5 | keep KO star/filter | `KoStarFilterHaveTest` still green |
| 0–1 | keep six menus | File/Edit/View/Tools/Window/Help |
| 5 | keep measure persist | `MeasureToolBarHaveTest.distanceStaysOnImagePixelsAfterZoomAndStillPaints` |
| 4 | keep VOI 0/1 overlay Have | Not touched. Chest: **0** → W:**32767**; **1** → W:**65535**. |
| 3/5 | keep KO/PR/SEG/RT corner icons | Not touched. |
| 4/6 | keep overlay Play/Stop + TOP tabs | Not touched. |
| 6 | explorer dock uncloseable; hang 1×2 stays on viewer-tabs in CWorkingArea; remaining series tab-drag/`splitSeries` uses `CLocation.working()`; Ctrl+E still own CDockable | hang cells are not east-split |
| 0–3, 6–15 | keep prior Have | Gogo 17179; jsvg shade/`bootdelegation` unchanged |
| layout | 985/985 | Remaining documented gaps: headed tutorials (§6 Pass empty; hang 1×2 / View 2×2 / explorer-row fill already PASS on `0b62ce6`); `AffineTransformOp` stays paint-time (zoom/pan/rotation); Q/R calling-AE persist is not spec'd. Empty braces gone ≠ Pass. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green locally (`NativeDistributionHaveTest` 2/2); `spotless:check` green; `gogo-smoke` `SMOKE_OK` on **GOSH_PORT=17279** (headed `0b62ce6` jar still holds default **17179**; codec/explorer/viewer2d Active; documented host i18n Resolved/Installed @13); `scripts/dicomizer-gogo-smoke.sh` `DICOMIZER_SMOKE_OK` (17181, explorer Installed missing mpr expected); `python3 scripts/source-surface-report.py` `SURFACE_OK` 985/985. Documented `mvn -P compressXZ -f weasis-distributions package` wrote `weasis-native.zip`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Empty braces gone ≠ Pass. Did not copy nroduit/Weasis method bodies. Did not invent preference keys. Did not invent Q/R calling-AE persist. Did not change desktop Gogo **17179**. Did not rename `explorer-series`. Did not reorder Help Keyboard Shortcuts / About / Licenses / System resources. Did not regress hang 1×2, layout `-n`, explorer-row fill, VOI LUT Sequence/SIGMOID, remaining-tab `CLocation` split, explorer filter chrome, Help shortcuts + System resources, KO apply/filter, explorer badges, Dummy off, clone-only TEXT/DATE/MODALITY, `dicom:close` model removal, `weasis:ui` `-q`/`-v`, `acquire:patient` XML, image-op process already Have, or i18n fragments @13. Composer relaunch **no** (`weasis-native.zip` is not hang/2×2/fill; hang/2×2/fill already PASS on `0b62ce6`). Methods touched this tick split so McCabe CRAP stays below 30 (`refuseSnapshot` CC **4** → CRAP@0% **16**; `requireZipAndLauncher` CC **4** → CRAP@0% **16**; `putOptionalTree` CC **3** → CRAP@0% **9**; `write(..., version)` CC **2** → CRAP@0% **4**).
