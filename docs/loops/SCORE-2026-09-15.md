# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior Space/I InfoLayer (`1910406`) | Kept. FULL → MINIMAL → HIDDEN still Have. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. |
| This tick histogram / LayerType / KO chrome | Independent Have from weasis.org Histogram + KO shortcuts: bind view → bins/peak/RGB; `LayerType` enum; Key Object star/filter + K. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–4, 7–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; JAXB-OSGi Active @7 |
| 5 | KO star/filter + K | Toolbar buttons toggle current SOP; filter lists only key images; shortcut K uses `ActionW.KO` |
| 6 | histogram dock | `HistogramView.bind` accumulates luminance; RGB/HSV/HLS fill `ChannelHistogramPanel`; selected closed graphic is ROI; W/L output changes bins; modality LUT on X axis |
| layout | 985/985 | `LayerType` is no longer empty. Matching a path is not feature-complete. Remaining: `LayerItem`/`LayerAnnotation` still empty; polyline double-click Pass. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, weasis-core Active @12, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
