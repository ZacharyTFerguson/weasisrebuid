# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior acquire explorer thumbnails / import / publish (`d96c610`) | Kept. Import/central thumbnail, grouping, and publish Have tests still green in this tick’s `mvn test`. |
| Prior acquire calibration + metadata (`825beb0`) | Kept. Calibration mm/pixel and metadata table Have tests still green. |
| Prior acquire editor + ArcBall (`9e8739e`) | Kept. Acquire/ArcBall Have tests still green. |
| Prior MPR `VolImageIO` / `RawImageIO` (`2da525e`) | Kept. Slice IO Have tests still green. |
| Prior PersonName/AnatomicRegion editors + annotate / metadata / action chrome (`2b973be`) | Kept. PersonName, AnatomicRegion, annotate, metadata gate, and action/submit chrome Have tests still green. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. WP-7 sibling invents `CprBuilder`/`Mip2d`; other `*-3711` branches invent `AuPlayer` (not in the 4.7.3 inventory). |
| This tick acquire browse/series chrome + `EditionToolFactory` | Independent Have: `FileSystemDrive` lists stills under a filesystem path; `BrowsePanel` selects a `MediaSource`; `ImportPanel` kicks `ImportTask`/`AcquireImportDialog` grouping into the central album; `SeriesButton`/`AcquireTabPanel` switch series groups; `EditionToolFactory` produces `EditionTool` named Edition. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–11, 13–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; docking-frames Active @10; DICOM Audio Player Active @75 |
| 7 | keep VolImageIO / RawImageIO | Orthogonal (+ MIP thickness) samples; signed-16 raw file round-trip |
| 12 | keep prior editor/meta/publish; add browse/series chrome | `FileSystemDrive`/`MediaSource` still listing; `BrowsePanel` source combo; `ImportPanel`→`ImportTask` grouping; `SeriesButton`/`AcquireTabPanel` album switch; `EditionToolFactory` → Edition; `AcquireActionPanel`/`AbstractAcquireActionPanel` session bind |
| layout | 985/985 | Matching a path is not feature-complete. Remaining thin types include `SeriesGroup`, `AcquireNewSeriesDialog`, `MediaImporterFactory`, `Global`/`DefaultTaggable`/`AcquireMediaInfo`/`AcquireImageStatus`, `AbstractBean`, `AcquireObject`, `OpValueChanged`, `AutoLevelListener`, `SeriesDataListener`. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, docking-frames Active @10, weasis-core Active @12, DICOM Audio Player Active @75, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
