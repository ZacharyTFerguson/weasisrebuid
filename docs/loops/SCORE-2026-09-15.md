# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior 3D chrome (`a56523c`) and `image:get` (`af10e06`) | Kept. MX-15 still gates VR; Gogo `image:get` Have still on smoke. |
| This tick JAXB graphics | Independent Have: marshal uses Jakarta JAXB, not DOM; legacy `<graphic type=…>` still unmarshals. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–4, 6–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; JAXB-OSGi Active @7; weasis-core Active @12 with jakarta.xml.bind import |
| 5 | JAXB graphic XML | `XmlGraphicModel` JAXB round-trip; `@XmlRootElement` on measure/draw types; uuid/lineThickness/`pt` attributes; legacy DOM fallback |
| layout | 985/985 | Matching a path is not feature-complete. Remaining: many core UI stubs. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, weasis-core Active @12, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis JAXB annotation bodies. Did not treat layout Have as Weasis-complete.
