# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior fusion LUT/window (`f5c0010`) | Kept. Fusion Have tests still green in this tick’s `mvn test`. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. |
| This tick acquire editor + ArcBall | Independent Have on fixture `AcquireAction` / `RectifyAction` / `ContrastAction` / `CropRectangleGraphic` / `ArcBallController`. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–6, 8–11, 13–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; docking-frames Active @10; DICOM Audio Player Active @75 |
| 7 | Shoemake ArcBall | `ArcBallController` drag rotates `AxesControl` `u`/`v`; east drag 90° about +Y sends `u` to −Z; axes feed `ObliqueMpr` |
| 12 | acquire editor pipeline | `AcquireAction` rotate → crop → contrast; rectify/contrast chrome writes `AcquireImageValues`; 90° snap |
| layout | 985/985 | Matching a path is not feature-complete. Remaining thin types include acquire explorer GUI, calibration/annotate panels, MPR `VolImageIO`/`RawImageIO`. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, docking-frames Active @10, weasis-core Active @12, DICOM Audio Player Active @75, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
