# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior histogram / LayerType / KO (`0230ede`) | Kept. Histogram bind/RGB/ROI and KO star/filter still Have. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. |
| This tick 3D cursor / Display layers | Independent Have: H/crosshair PixelInfo; FoR vs manual synch of the point; `LayerItem`/`LayerAnnotation` no longer empty. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–5, 7–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; JAXB-OSGi Active @7 |
| 6 | 3D cursor + Display layers | Shortcut H selects crosshair; click writes PixelInfo (raster + modality LUT); FoR peers follow, manual peers do not (and the reverse); CROSSLINES hide stops paint; patient `LayerAnnotation` item can drop the name from FULL overlay |
| layout | 985/985 | Matching a path is not feature-complete. Remaining: polyline double-click Pass; other thin types (`GraphicModelChangeListener`, 3D overlay-only). |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, weasis-core Active @12, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
