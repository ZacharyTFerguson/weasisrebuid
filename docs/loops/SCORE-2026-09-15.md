# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior RT Structure Set contours (`1c7933f`) | Kept. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. `wp-7-mpr-cpr-mip-c86f` unique commit adds `CprBuilder`/`Mip2d`/`MprVolume` paths that are **not** in the 4.7.3 inventory (would break SURFACE_OK). |
| This tick CPR / oblique / cross-section | Independent Have on fixture types `cmpr/*`, `ObliqueMpr`, `AxesControl`, `AxisDirection`. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–6, 8–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; docking-frames Active @10; RT Tools Active @115 |
| 7 | CPR + oblique | `CurveSampler` uniform arc-length; straightened in-plane CPR; `ObliqueMpr` origin+u+v matches axial when aligned; curve-perpendicular `CrossSectionImageIO` / dialog preview |
| layout | 985/985 | Matching a path is not feature-complete. Remaining thin types include acquire GUI, fusion volume builders, `ArcBallController`. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, docking-frames Active @10, weasis-core Active @12, DICOM RT Tools Active @115, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
