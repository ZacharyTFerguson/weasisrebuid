# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior JAXB graphics (`aa33881`) | Kept. Core still Active @12 with jakarta.xml.bind; marshal is JAXB not DOM. |
| Stale sibling `cursor/wp-*-c86f` unique commits | Not merged. Older WP lineage already on integration; cherry-picking would regress. |
| `cursor/*-3711` | Not merged. Different oracle experiment. |
| This tick ViewerToolBar / cine / stills ImportToolBar | Independent Have: toolbar tokens match COMMANDS.md; cine ticks frames and series pixels; ImportToolBar uses `image:get -f` ImageIO. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–3, 5–12, 14–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; JAXB-OSGi Active @7; Docking Frames Active @10; jogamp Active @120 |
| 4 | ViewerToolBar + cine | `ViewerToolBar` sets `dcmview2d:mouseLeftAction` tokens (including `sequence`); `CineToolBar` Play/Stop + `SliderCineListener.tick` loops frames and fires `SynchCineEvent`; series `ImageElement` pixels swap |
| 13 | stills ImportToolBar | empty `ImportToolBar` is now `WtoolBar`; local files via ImageIO `image:get -f` |
| layout | 985/985 | Matching a path is not feature-complete. Remaining: other core UI stubs (`MeasureToolBar` still has no type buttons; base `EventManager`/`InfoLayer`/`ImageTool`/`DisplayTool` empty). |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, weasis-core Active @12, Base 2D Viewer Active @100, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
