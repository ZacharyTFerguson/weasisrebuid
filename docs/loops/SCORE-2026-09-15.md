# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior CPR / oblique (`f7ef7b8` / SCORE `80b3fb4`) | Kept. Fixture `cmpr/*` + `ObliqueMpr`; `CprObliqueHaveTest` still green in this tick’s `mvn test`. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. `wp-8-fusion-c86f` is old `targetViews` on a base that also carries non-fixture WP-7 types. |
| This tick PET/CT fusion volumes | Independent Have on fixture fusion builder/matcher/resampler/registration/compatibility types. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–7, 9–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; docking-frames Active @10 |
| 8 | PET/CT volume resample | Stack by IPP Z; nearest overlay slice; resample onto denser CT Z; voxel-shift 4×4; same FoR + overlapping Z; `FusionOp` blend |
| layout | 985/985 | Matching a path is not feature-complete. Remaining thin types include acquire GUI, `ArcBallController`, AU player, fusion LUT/window chrome. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, docking-frames Active @10, weasis-core Active @12, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
