# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior 3D cursor / Display layers (`5ea452f`) | Kept. |
| Graphics select/delete (`4f21057`) plus rubber-band test (`c640b7c`) | Kept after independent fix: horizontal `LineGraphic` selection uses `Shape.intersects` (zero-height bounds skipped the rubber-band). Rubber-band only when left is `none` so default W/L drag is not a `SelectGraphic`. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–3, 7–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; JAXB-OSGi Active @7; docking-frames Active @10 |
| 4 | T/W/S/Z/R/C/Q/P + CalibrationView | Keys set left-mouse tokens; C toggles cine; Q builds context menu; P rasterizes `ImagePrint`. Session Manual Calibration does not write monitor pitch (MX-07). |
| 5 | graphics select (kept + line intersect) | Click / Shift+click / rubber-band / Ctrl+A/D / Delete. Polyline double-click still not headed Pass. |
| 6 | Mini Tool + panner + region stats | Zoom/rotation sliders; panner `centerOnImage`; `ImageRegionStatistics` on selected closed graphic. |
| layout | 985/985 | Matching a path is not feature-complete. Remaining thin types include `GraphicsPane`. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, docking-frames Active @10, weasis-core Active @12, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
