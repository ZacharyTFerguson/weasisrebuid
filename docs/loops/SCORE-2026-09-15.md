# SCORE 2026-09-15 (independent review tick)

Integration: `cursor/weasis-spec-rebuild-26c5`. Spec: `docs/weasis-spec/`. §6 Pass stays empty.

## Team deltas reviewed (not rubber-stamped)

| Source | Verdict |
|---|---|
| Prior MeasureToolBar draw (`5c95f97`) | Kept. Distance click-drag-release and angle second-click still Have. |
| Stale sibling `cursor/wp-*-c86f` and `*-3711` | Not merged. |
| This tick Space/I InfoLayer | Independent Have: three visibility states; Display/Image docks bind the layer; Space and I both map to `ActionW.ANNOTATIONS`. |

## Have vs WORK-PACKAGES

| WP | Have this tick | Notes |
|---|---|---|
| 0–3, 5, 7–15 | keep prior Have | Gogo 17179; codec/explorer/viewer2d Active; JAXB-OSGi Active @7; MeasureToolBar unchanged |
| 4 | annotation 3-state | Space/I cycles FULL → MINIMAL → HIDDEN; DICOM overlay drops patient on MINIMAL; stills overlay drops zoom on MINIMAL |
| 6 | Display/Image docks | `DisplayTool` and `ImageTool` are Insertable TOOL panes; InfoLayer is no longer an empty class |
| layout | 985/985 | Matching a path is not feature-complete. Remaining: histogram dock is still a thin panel; `LayerType` is empty; polyline double-click Pass. |

Source-surface: present **985/985**, clone-only 87, unexpected 0, missing **0**, `SURFACE_OK`.

Gates this tick: `mvn test` green; `spotless:check` green; `gogo-smoke` `SMOKE_OK` (Felix 7.0.5, start 130, codec/explorer/viewer2d Active, JAXB-OSGi Active @7, weasis-core Active @12, `image:get` Have); `python3 scripts/source-surface-report.py` `SURFACE_OK`.

## Forbidden

Did not tick CHECKLIST §6 Pass. Did not copy nroduit/Weasis method bodies. Did not treat layout Have as Weasis-complete.
