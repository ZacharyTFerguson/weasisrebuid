<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.weasis</groupId>
    <artifactId>weasis-parent</artifactId>
    <version>${revision}${changelist}</version>
    <relativePath>../weasis-parent/pom.xml</relativePath>
  </parent>

  <artifactId>weasis-pacs-connector</artifactId>
  <packaging>jar</packaging>
  <name>Weasis ViewerHub / PACS connector</name>
  <description>WP-15 Have: ViewerHub GET /display and dcm4chee IID emit weasis://. Sister of nroduit/viewer-hub; not a Felix auto-start bundle and not in the Weasis 4.7.3 main POM fixture.</description>

  <dependencies>
    <dependency>
      <groupId>org.weasis</groupId>
      <artifactId>weasis-launcher</artifactId>
      <version>${revision}${changelist}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>biz.aQute.bnd</groupId>
        <artifactId>bnd-maven-plugin</artifactId>
        <configuration>
          <bnd><![CDATA[
Bundle-SymbolicName: org.weasis.connector
Bundle-Name: Weasis ViewerHub connector
Export-Package: org.weasis.connector
Import-Package: *
]]></bnd>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
