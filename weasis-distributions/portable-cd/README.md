# Portable / CD sibling folders

Native zip and Windows portable archives auto-open DICOM from these directories beside the
binary (`weasis.portable.dicom.directory` = `dicom,DICOM,IMAGES,images`).

`$dicom:get -p` walks the same names relative to `--from-target` / `weasis.portable.root`.
